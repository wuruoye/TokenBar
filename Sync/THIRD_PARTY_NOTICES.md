# Third-party notices

TokenBar Sync uses the repository's `tokenbar-helper` crate and the Rust dependencies resolved in `Sync/Cargo.lock`. TokenBar is distributed under the repository-root MIT license. Dependency license metadata should be regenerated through the project's established release/compliance process when Sync is packaged for distribution.
