## Build, Install, and Launch

- 当 agent 修改本项目的代码或应用资源后，在交付前自动运行与改动相匹配的测试；测试通过后执行 `./Scripts/package_app.sh`，将应用打包到仓库根目录的 `TokenBar.app`，并重新启动该应用，让用户可以直接体验最新改动。
- 纯问答、代码审查、状态查询和只修改文档时不触发打包安装。用户明确要求暂不编译、暂不安装或暂不重启时，以用户要求为准。
- 打包、签名或启动失败时不得宣称已经安装；应报告首个明确错误和完整日志路径。
